document.addEventListener("DOMContentLoaded", () => { const menuBtn = document.getElementById("menu-btn"); const nav = document.querySelector(".navbar"); menuBtn.addEventListener("click", () => { nav.classList.toggle("open"); }); const featured = document.getElementById("featured"); if (featured) { featured.innerHTML = `<div class="card"><h2>Featured Profile</h2><p>Check out Murudo’s amazing members!</p></div>`; } }); const featuredSection = document.getElementById("featured"); const featuredProfiles = [ { name: "Anna", location: "Harare", image: "images/anna.jpg" }, { name: "James", location: "Bulawayo", image: "images/james.jpg" }, { name: "Lerato", location: "Kwekwe", image: "images/lerato.jpg" } ]; featuredProfiles.forEach(profile => { const card = document.createElement("div"); card.classList.add("profile-card"); card.innerHTML = ` <img src="${profile.image}" alt="${profile.name}"> <h3>${profile.name}</h3> <p>${profile.location}</p> `; featuredSection.appendChild(card); });